void backtracing(arg) {
    if (terminal condition) {
        save result;
        return;
    }
    for (selection: this level) {
        deal nodes;
        backtracing(arg);
        undo the results of dealing;    
    }
} 
